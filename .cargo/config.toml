# Cross-compilation targets for edge deployment.
# Install the target with: rustup target add aarch64-unknown-linux-gnu
# Install the linker with: brew install aarch64-linux-gnu-gcc (macOS)

[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
